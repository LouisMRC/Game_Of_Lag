#include <SFML/Graphics.hpp>
#include <array>
#include "Button.hpp"

int main()
{
    int generation = 0;
    sf::RenderWindow win(sf::VideoMode(800, 600), "Game Of Lag");

    while (win.isOpen())
    {
        sf::Event event;
        while(win.pollEvent(event))
        {

        }
    }
    
}